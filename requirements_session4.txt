# Session 4 — Production Optimiser
# Install: pip install -r requirements.txt

# Core LP solver (COIN-BC bundled — no external binary needed)
pulp>=2.7.0

# API framework (already present from Sessions 1-3)
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0

# Testing
pytest>=8.0.0
httpx>=0.27.0   # for FastAPI TestClient

# Scientific (optional — SciPy fallback for LP if PuLP unavailable)
scipy>=1.12.0

# Already required by Sessions 1-3
pandas>=2.0.0
numpy>=1.26.0
